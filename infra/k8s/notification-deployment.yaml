apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  namespace: faithfit
spec:
  replicas: 2
  selector: { matchLabels: { app: notification } }
  template:
    metadata: { labels: { app: notification } }
    spec:
      containers:
        - name: notification
          image: ghcr.io/faithfit/notification:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: faithfit-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: notification, namespace: faithfit }
spec:
  selector: { app: notification }
  ports: [{ port: 80, targetPort: 4000 }]
