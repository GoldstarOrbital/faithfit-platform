apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-profile
  namespace: faithfit
spec:
  replicas: 2
  selector: { matchLabels: { app: user-profile } }
  template:
    metadata: { labels: { app: user-profile } }
    spec:
      containers:
        - name: user-profile
          image: ghcr.io/faithfit/user-profile:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: faithfit-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: user-profile, namespace: faithfit }
spec:
  selector: { app: user-profile }
  ports: [{ port: 80, targetPort: 4000 }]
