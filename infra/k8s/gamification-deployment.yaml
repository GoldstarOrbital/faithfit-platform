apiVersion: apps/v1
kind: Deployment
metadata:
  name: gamification
  namespace: faithfit
spec:
  replicas: 2
  selector: { matchLabels: { app: gamification } }
  template:
    metadata: { labels: { app: gamification } }
    spec:
      containers:
        - name: gamification
          image: ghcr.io/faithfit/gamification:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: faithfit-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: gamification, namespace: faithfit }
spec:
  selector: { app: gamification }
  ports: [{ port: 80, targetPort: 4000 }]
