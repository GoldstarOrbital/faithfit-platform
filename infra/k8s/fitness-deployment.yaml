apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitness
  namespace: faithfit
spec:
  replicas: 2
  selector: { matchLabels: { app: fitness } }
  template:
    metadata: { labels: { app: fitness } }
    spec:
      containers:
        - name: fitness
          image: ghcr.io/faithfit/fitness:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: faithfit-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: fitness, namespace: faithfit }
spec:
  selector: { app: fitness }
  ports: [{ port: 80, targetPort: 4000 }]
