apiVersion: apps/v1
kind: Deployment
metadata:
  name: creator-tools
  namespace: functioning-faith
spec:
  replicas: 2
  selector: { matchLabels: { app: creator-tools } }
  template:
    metadata: { labels: { app: creator-tools } }
    spec:
      containers:
        - name: creator-tools
          image: ghcr.io/functioning-faith/creator-tools:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: functioning-faith-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: creator-tools, namespace: functioning-faith }
spec:
  selector: { app: creator-tools }
  ports: [{ port: 80, targetPort: 4000 }]
