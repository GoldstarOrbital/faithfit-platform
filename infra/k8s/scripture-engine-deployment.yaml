apiVersion: apps/v1
kind: Deployment
metadata:
  name: scripture-engine
  namespace: functioning-faith
spec:
  replicas: 2
  selector: { matchLabels: { app: scripture-engine } }
  template:
    metadata: { labels: { app: scripture-engine } }
    spec:
      containers:
        - name: scripture-engine
          image: ghcr.io/functioning-faith/scripture-engine:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: functioning-faith-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: scripture-engine, namespace: functioning-faith }
spec:
  selector: { app: scripture-engine }
  ports: [{ port: 80, targetPort: 4000 }]
