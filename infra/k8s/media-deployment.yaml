apiVersion: apps/v1
kind: Deployment
metadata:
  name: media
  namespace: functioning-faith
spec:
  replicas: 2
  selector: { matchLabels: { app: media } }
  template:
    metadata: { labels: { app: media } }
    spec:
      containers:
        - name: media
          image: ghcr.io/functioning-faith/media:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: functioning-faith-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: media, namespace: functioning-faith }
spec:
  selector: { app: media }
  ports: [{ port: 80, targetPort: 4000 }]
