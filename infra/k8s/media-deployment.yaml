apiVersion: apps/v1
kind: Deployment
metadata:
  name: media
  namespace: faithfit
spec:
  replicas: 2
  selector: { matchLabels: { app: media } }
  template:
    metadata: { labels: { app: media } }
    spec:
      containers:
        - name: media
          image: ghcr.io/faithfit/media:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: faithfit-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: media, namespace: faithfit }
spec:
  selector: { app: media }
  ports: [{ port: 80, targetPort: 4000 }]
