apiVersion: apps/v1
kind: Deployment
metadata:
  name: personalization
  namespace: faithfit
spec:
  replicas: 2
  selector: { matchLabels: { app: personalization } }
  template:
    metadata: { labels: { app: personalization } }
    spec:
      containers:
        - name: personalization
          image: ghcr.io/faithfit/personalization:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: faithfit-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: personalization, namespace: faithfit }
spec:
  selector: { app: personalization }
  ports: [{ port: 80, targetPort: 4000 }]
