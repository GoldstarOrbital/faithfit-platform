apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-graph
  namespace: functioning-faith
spec:
  replicas: 2
  selector: { matchLabels: { app: social-graph } }
  template:
    metadata: { labels: { app: social-graph } }
    spec:
      containers:
        - name: social-graph
          image: ghcr.io/functioning-faith/social-graph:latest
          ports: [{ containerPort: 4000 }]
          envFrom:
            - secretRef: { name: functioning-faith-secrets }
          readinessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health, port: 4000 }, initialDelaySeconds: 15 }
---
apiVersion: v1
kind: Service
metadata: { name: social-graph, namespace: functioning-faith }
spec:
  selector: { app: social-graph }
  ports: [{ port: 80, targetPort: 4000 }]
