global: { scrape_interval: 15s }
scrape_configs:
  - job_name: faithfit-services
    kubernetes_sd_configs:
      - role: pod
        namespaces: { names: [faithfit] }
    relabel_configs:
      - source_labels: [__meta_kubernetes_pod_label_app]
        target_label: service
