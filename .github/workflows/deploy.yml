name: Deploy

on:
  workflow_dispatch:
  push: { tags: ['v*'] }

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Helm upgrade
        run: |
          echo "helm upgrade --install faithfit infra/helm/faithfit -n faithfit --values infra/helm/faithfit/values.yaml"
          echo "(placeholder - wire real kubeconfig/OIDC auth before enabling)"
