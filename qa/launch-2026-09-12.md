# September 12 launch readiness

## September 6, performance and feed pass (baseline `b3a7346`)

Verified against a running server by `webapp/scripts/smoke-api.js`, 25/25.
That script needs a live instance and deletes the account it signs in as, so
it is a deliberate pre-release check against a scratch instance, not CI.

**Nothing in this section has been seen running on a device.** See
`qa/simulator-plan.md`, which is ordered by risk for a ~100 minute budget.

Opening the app:

- Home paints the last feed on the first frame instead of a spinner. The
  network fetch still always runs and wins, so there is no staleness window;
  the stored copy only decides what is on screen while it is in flight.
- Reels stopped making third-party calls inline. It was fetching the YouTube
  API and scraping the member's church website on *every* open, so a slow
  church site made Reels slow every time. Now cached per church, refreshed in
  the background, never awaited.
- The service worker was precaching 14 URLs nobody requests while the 15 the
  page actually loads were precached by nothing — asset fingerprinting had
  broken the hand-written list silently. Generated from the real list now, and
  content-hashed assets are cacheable for a year again.

Scripture offline:

- The whole Bible ships as one payload: 31,202 verses, ~1.2MB over the wire,
  fetched once in the background and kept in Application Support. Previously
  only chapters opened while online were readable offline.
- Verified with the server stopped: a chapter already read still opens.

Feed sources — both were asked for and neither existed:

- A finished workout posts with a verse matched to the activity. A 5.2 km run
  produced Colossians 1:11.
- A verse conversation posts the first time someone reflects on it. Once per
  thread, not once per reflection.
- Both publish at the member's own `default_visibility`, so neither can
  publish wider than the member already chose, and under-18 accounts stay
  private.

Corrections to earlier claims in this document's own history, both found by
testing rather than re-reading:

- The feed is **not** `no-store`. It is `private, max-age=15`, set in
  `server.js` and predating this pass. The earlier claim came from a `curl -I`
  HEAD request, and that middleware only applies to GET.
- Because of that, composing a post could reload the feed from a copy taken
  before the post existed, so a member's own post appeared to fail to publish.
  Now bypassed after composing, on pull to refresh, and on retry.

Still open from the reported list: search (server and decoding both verified
correct, so it is the field itself — the top item in the simulator plan), and
church → weekly sermon sync, which is the only remaining item that is new
surface area rather than a fix.

## September 6 verified follow-up

- App Store Connect directly shows app build `1788676008` (Codemagic #140,
  commit `0e290f6`) as **Approved**, assigned to Goldstar Orbital with three
  invitations. Installation count is unavailable, not proof of an install.
- Latest baseline Railway status is successful; health is 200. Compact public
  feed is 9,092 bytes, compared with the previous 7,146,451-byte inline payload.
- Deferred video loaded into the production web player; formatted Journey
  progress and the newer web bundle were visibly verified.
- Added a web inbox New message picker, filtering to people and excluding self;
  stale/cleared search responses and detached inbox responses are guarded.
- Found a second freshness layer: HTTP immutable caching of reused script URLs
  could bypass the service-worker fix. The server now content-hashes local
  script/style URLs in the app shell and revalidates executable assets with ETag.
- The user approved the free Codemagic App Preview trial, but activation reports
  that a team account is required. No billing or application ownership was changed.
- Native hardware QA and App Store submission metadata remain open. The app's
  App Store distribution version still says Prepare for Submission; TestFlight
  approval is not App Store release approval.

## Current state (September 5, later pass)

Baseline moved from `4ec3c74` to `6a8bb6d`. Compilation and Web CI were checked
in that pass. Later live verification found TestFlight #139 failed; #140
successfully processed and was approved. Do not infer distribution success
from compilation or IPA creation alone.

Safety fixes in this pass, all with regression tests now running in Web CI:

- **Blocking did not end the relationship.** Every read path honoured a block,
  but `POST /users/:id/follow` had no check at all, so a blocked member could
  follow the person who blocked them back into the `followers` tier that gates
  followers-only content — or, on a private account, file a request that
  notifies the target by name. An existing mutual follow, a pending request and
  circle membership all outlived a block. `dms.block()` now severs all three
  both ways.
- **Verse threads had no block handling in either direction.** A blocked
  member's reflections stayed visible to the blocker, replying delivered the
  sender's name plus 60 characters of their own text into the blocker's
  notifications, and liking notified the author. Group pulse encouragement and
  event RSVP had the same unguarded notification.
- **Workout numbers a member submits were unbounded.** `distance_km` feeds the
  community leaderboard, challenge and journey progress, personal bests and
  stats totals; five routes wrote it, four without a ceiling, the easiest being
  a number typed into the manual-entry form. A non-numeric value was stored as
  text by SQLite and later threw on `.toFixed()` when the workout was shared to
  a DM. `duration_min`, the second ranked metric, was clamped at zero but not
  at the top.
- **Home's Notifications screen** stayed presented after a notification tap
  switched tabs, leaving Home's navigation state out of sync with its own shell.

Verified during the sweep, no change needed: age gating is consistent across
both signup paths (each rejects under-13 and applies the same private-by-default
settings for under-18); account deletion nulls the session the same way logout
does; the service worker's shell list matches `index.html`; the deferred-media
endpoint re-checks visibility on every fetch.

Open product question (resolved Sept 8): Alex decided mute hides stories.
Stories now filter mutes the same way the feed does (account_relationship_controls
control='mute'), alongside the existing dm_blocks filter. Mute copy mentions stories.

## September 5 audit baseline

- GitHub baseline: `4ec3c74` (clean working tree before this audit).
- Codemagic TestFlight workflow #129, app build `1788655727`: Apple processing completed; external beta submission reported WAITING_FOR_REVIEW. This is not App Store approval.
- Recent changes include new section navigation, For You, notification/deep-link destinations, profile moderation controls, Bible Answers, Health sync, Spotify handoff, and inline Originals playback.
- Production health returned 200 in 0.184 seconds in one local measurement.
- Public feed returned 200 with 7,146,451 bytes in 0.567 seconds on this connection. This is not a mobile-network benchmark.

## Fixes in this batch

- Guard feed refresh/pagination with request generations so obsolete responses cannot replace another mode or append to a refreshed feed.
- Resolve Reel identity after reaction requests complete; never reuse a pre-request array index after hide/refresh.
- Apply returned like counts to Reels.
- Rank For You using metadata; load media and GPS only for authorized winners, not all 200 candidates. Response remains compatible with installed clients.
- Format home Journey progress instead of displaying raw floating-point precision.
- Update stale navigation/vertical-scroll contract checks; allow full-canon test DB initialization before authentication integration checks.
- Add executable SQLite feed tests for candidate size, media hydration, visibility, blocks, and mutes to Web CI.
- Fix deployment freshness: unchanged asset version strings plus cache-first service worker behavior kept returning old JavaScript. Bump the shell/app version and revalidate assets through HTTP validators while retaining offline fallback.

## Remaining launch gates (not claimed complete)

1. Deferred media is now implemented for the web and new iOS feed: photos load near visibility and videos on demand through a protected, non-cached endpoint. Older clients retain inline media. Verify production payload reduction and signed-device playback. Long-term object storage and streamed/range video delivery remain separate scalability work.
2. Test signed iPhone build: fast feed switching, refresh while reacting/hiding Reels, all section entry/back paths, DM compose/profile selection, photo/MOV uploads, and persisted settings.
3. Device-only tests: HealthKit read/write consent, real GPS outdoors, background workout recovery, Live Activity, Bluetooth sensors, heart-rate notification thresholds. Browser checks cannot certify these.
4. Validate workout completion summary against recorded data and verify no
   invented distance/pace when GPS is missing. Partly settled by review: the
   iOS summary already returns nil rather than inventing either when GPS is
   absent, and the server no longer accepts an unbounded or non-numeric
   distance. What remains is device-only — confirm a real recorded session's
   summary matches what was actually tracked.
5. Confirm this batch's exact commit compiles and finishes Apple processing;
   record TestFlight build number and tester availability separately from
   upload success. Done through `6a8bb6d`: every commit built and produced an
   IPA. Tester availability is still unconfirmed — an upload is not a build a
   tester can open.
6. Before September 12: finalize App Store privacy disclosures, moderation/report/block and account deletion checks, screenshots and review metadata; submit a release candidate with time for Apple's review. Approval timing is external and cannot be guaranteed.

## What only a person with hardware or the Apple account can do

Everything above that is still open falls into one of these two. No amount of
further code review moves them, and gate 6 is the one with a deadline that
cannot be compressed — Apple's review queue runs on its own clock, so the
submission date, not the code-freeze date, is what September 12 depends on.

Needs a device (a physical iPhone on TestFlight, or a reachable Mac + simulator):

- Gates 2 and 3 in full, plus the device half of gate 4.
- Confirm the Search field renders. Its root cause was found and fixed
  (`.searchable()` was nested inside a conditional, which stops it registering
  with the navigation bar at all), but no one has yet seen it appear.
- Confirm the toolbar and navigation fixes: Home's compose and notification
  buttons, the Reels create button, the Messages new-message button, and that
  leaving a section pops its stack.
- Confirm blocking end to end. The server side is now covered by tests, but the
  member-facing flow — block from a profile, verify they vanish from your
  threads, feed and notifications — has never been run.

Needs the Apple Developer / App Store Connect account:

- Gate 6 in full: privacy disclosures, screenshots at each required size,
  description, keywords, age rating, support and marketing URLs.
- Confirm a TestFlight build is actually installable by a tester, which is
  distinct from the upload succeeding.

## Release rules

Push scoped changes to GitHub main. Railway and Codemagic trigger from that push; do not run `railway up` or start duplicate builds. Do not store signing/API credentials or rendered marketing videos here.
