name: FunctioningFaith
options:
  minimumXcodeGenVersion: 2.38.0
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: 5.9
    # Required for the app and its Live Activity extension to archive with
    # their matching App Store provisioning profiles on CI.
    DEVELOPMENT_TEAM: P3999S6HG4
targets:
  FunctioningFaith:
    type: application
    platform: iOS
    sources:
      - path: FunctioningFaith
        excludes:
          - Resources/Info.plist
          - Resources/FunctioningFaith.entitlements
    entitlements:
      path: FunctioningFaith/Resources/FunctioningFaith.entitlements
    dependencies:
      - target: FunctioningFaithWidgets
        embed: true
    postBuildScripts:
      - name: Preserve App Store icon metadata
        script: |
          set -euo pipefail
          plist="$TARGET_BUILD_DIR/$INFOPLIST_PATH"
          plistbuddy=/usr/libexec/PlistBuddy
          "$plistbuddy" -c 'Delete :CFBundleIconName' "$plist" || true
          "$plistbuddy" -c 'Add :CFBundleIconName string AppIcon' "$plist"
          for icon in AppIcon60x60@2x.png AppIcon60x60@3x.png AppIcon76x76@2x.png AppIcon83.5x83.5@2x.png; do
            cp "$SRCROOT/FunctioningFaith/Resources/$icon" "$TARGET_BUILD_DIR/$UNLOCALIZED_RESOURCES_FOLDER_PATH/$icon"
          done
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.functioningfaith.app
        # Keep the HealthKit entitlement attached even when the project is
        # regenerated on CI; the entitlement file alone is not enough if a
        # generated build setting ever drops its reference.
        CODE_SIGN_ENTITLEMENTS: FunctioningFaith/Resources/FunctioningFaith.entitlements
        INFOPLIST_FILE: FunctioningFaith/Resources/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        PRODUCT_MODULE_NAME: FunctioningFaith
        PRODUCT_NAME: FunctioningFaith
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_CFBundleIconName: AppIcon
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight
        "INFOPLIST_KEY_UISupportedInterfaceOrientations~ipad": UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight
        TARGETED_DEVICE_FAMILY: "1,2"
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_OPTIMIZATION_LEVEL: "-Onone"
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
      configs:
        Debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG
        Release:
          SWIFT_OPTIMIZATION_LEVEL: "-O"
  FunctioningFaithWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: FunctioningFaithWidgets
        excludes:
          - Info.plist
      - path: FunctioningFaith/Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.functioningfaith.app.widgets
        INFOPLIST_FILE: FunctioningFaithWidgets/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        PRODUCT_MODULE_NAME: FunctioningFaithWidgets
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
  FunctioningFaithTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: FunctioningFaithTests
    dependencies:
      - target: FunctioningFaith
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.functioningfaith.app.tests
        PRODUCT_MODULE_NAME: FunctioningFaithTests

schemes:
  FunctioningFaith:
    build:
      targets:
        FunctioningFaith: all
        FunctioningFaithTests: [test]
    test:
      gatherCoverageData: true
      targets:
        - FunctioningFaithTests
