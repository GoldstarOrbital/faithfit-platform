services:
  - type: web
    name: functioning-faith-demo
    runtime: node
    plan: free
    rootDir: demo-server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
